name: Testing vars
on:
   - push
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Read exported variable
        run: echo "Hello world" >> hello.txt
      - name: Run Python Script
        run: |
           export TEST=$(cat hello.txt)
           echo $TEST
    
        
        
